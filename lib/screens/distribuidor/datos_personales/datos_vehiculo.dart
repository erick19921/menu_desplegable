import 'package:flutter/material.dart';

class DatosVehicuoScreen extends StatelessWidget {
  const DatosVehicuoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DatosVehicuoScreen'),
      ),
    );
  }
}
